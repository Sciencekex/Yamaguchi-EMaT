# EMaT production API base URL
# When deploying the Python backend separately (e.g. on Render/Railway),
# set this to the backend URL.
# Example: VITE_API_BASE=https://emat-backend.onrender.com
# Leave empty to default to "/api" (same-origin, works with Vite proxy in dev)
VITE_API_BASE=
